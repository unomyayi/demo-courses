// Простой скрипт проверки статуса (пример)
document.addEventListener("DOMContentLoaded", () => {
    const statusEl = document.querySelector('.status');
    if (statusEl) statusEl.textContent = "онлайн";
  });
  